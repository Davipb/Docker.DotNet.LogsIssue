This repository is an example of [issue #359 in Docker.DotNet](https://github.com/Microsoft/Docker.DotNet/issues/359), regarding `GetContainerLogsAsync`.
To run this example, use:

```bash
dotnet run
```
